apiVersion: v1
kind: Service
metadata:
  name: nginx-efs-static-service
  namespace: default
spec:
  selector:
    app: nginx-efs-static
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP